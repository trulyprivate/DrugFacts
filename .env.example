# Environment Configuration for drugfacts.wiki

# Application
NODE_ENV=production
PORT=3000

# Database Configuration (PostgreSQL)
DATABASE_URL=postgresql://drugfacts_user:your_secure_password@localhost:5432/drugfacts
PGHOST=localhost
PGPORT=5432
PGUSER=drugfacts_user
PGPASSWORD=your_secure_password
PGDATABASE=drugfacts

# Redis Configuration (for session storage and caching)
REDIS_URL=redis://localhost:6379

# Security
SESSION_SECRET=your-super-secret-session-key-here-at-least-32-characters

# API Keys (if needed in the future)
# OPENAI_API_KEY=sk-...
# SENDGRID_API_KEY=SG...

# Domain Configuration
DOMAIN=drugfacts.wiki
ALLOWED_ORIGINS=https://drugfacts.wiki,https://www.drugfacts.wiki

# SSL Configuration (for production)
SSL_CERT_PATH=/etc/nginx/ssl/drugfacts.wiki.crt
SSL_KEY_PATH=/etc/nginx/ssl/drugfacts.wiki.key